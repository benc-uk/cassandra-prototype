cassandra:
  persistence:
    enabled: false

prometheus:
  server:
    persistentVolume:
      enabled: false

ingress:
  enabled: true
  hosts:
    - host: testapp.kube.benco.io
      paths: ["/"]
  tls:
    - secretName: kube-benco-io-cert
      hosts:
        - testapp.kube.benco.io
