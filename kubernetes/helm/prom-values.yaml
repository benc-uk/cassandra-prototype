prometheus:
  server:
    persistentVolume:
      enabled: false

grafana:
  service:
    type: ClusterIP
  ingress:
    enabled: true
    hosts: ["grafana.kube.benco.io"]
    tls:
      - secretName: kube-benco-io-cert
        hosts:
          - grafana.kube.benco.io
